#include "hal/hal.h"

void app_main()
{
    // startup
    hal_init();

    // main loop
    while (1)
    {
        // update systems

        // handle remote communications

        // sleep main task for remainder of loop cycle
    }
}